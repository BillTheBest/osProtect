<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>osProtect: Notification Results</title>
</head>
<body>
  <h2>Notification summary for <%= @user.username %>:</h2>
  <blockquote>
    <p>
      <strong><%= pluralize(@notification_result.total_matches, 'event') %></strong> were found matching your criteria within the
      time frame of <%= @notification_result.events_timestamped_from %> to <%= @notification_result.events_timestamped_to %>.
    </p>
    <p>
      To view the results for this notification, follow this link: <%= link_to('notification results', url_for(:action => 'index', :controller => 'notifications', only_path: false)) %>.
    </p>
  </blockquote>
</body>
</html>